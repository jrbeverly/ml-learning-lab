name: Build HTML and Deploy
on:
  push:
    branches:
      - main
jobs:
  build-html-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write

    environment:
      name: github-pages
      url: ${{ steps.build-html-and-deploy.outputs.page_url }}

    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2
        with:
          persist-credentials: false

      - name: Setup Miniconda
        uses: conda-incubator/setup-miniconda@v2
        with:
          auto-update-conda: true
          auto-activate-base: false
          miniconda-version: "latest"
          python-version: 3.8
          environment-file: environment.yml
          activate-environment: ml-learning-lab

      - name: Install jupyter_book
        shell: bash
        run: pip install git+https://github.com/ExecutableBookProject/jupyter-book.git@v0.10.1

      - name: Copy in the project README
        run: cp README.md notebooks/index.md

      - name: Build Notebooks
        shell: bash
        run: jb build notebooks/

      - name: Deployment Configuration Settings
        run: touch notebooks/_build/html/.nojekyll

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v1
        with:
          artifact_name: notebooks/_build/helm
